# Зависимости и сборка
node_modules/
app/node_modules/

# Артефакты Vite / Electron
app/dist/
app/dist-electron/

# Установщик и распакованная сборка (тяжёлые файлы)
app/release/

# Логи и служебное
*.log
.DS_Store
Thumbs.db
*.local
.env

# Временная ASCII-сборка
C:/spravochnik-build/

# graphify (локальные артефакты; graphify-out/ коммитится для карты кода)
graphify-out/cost.json
graphify-out/.graphify_python

# Экспорт данных для сервера (не коммитить — содержит accounts.json)
REST-INFO-export/
server/data/
import/REST-INFO-export/

# Deploy keys — только публичный .pub в deploy/keys/; приватный ключ только на сервере
deploy/keys/rest-info-deploy-key
deploy/keys/*_key
deploy/keys/id_*
!deploy/keys/*.pub
!deploy/keys/README.md
